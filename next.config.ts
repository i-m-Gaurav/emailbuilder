import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  //here write the config for all kind of image source from the internet.
  images: {
    domains: ['t4.ftcdn.net'],
  },
};

export default nextConfig;
